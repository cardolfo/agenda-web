<p id="notice"><%= notice %></p>
<p>
  <strong><%= t("activerecord.attributes.user.name") %>:</strong>
  <%= handle_field_value(@user.name) %>
</p>
<p>
  <strong><%= t("activerecord.attributes.user.email") %>:</strong>
  <%= handle_field_value(@user.email) %>
</p>
<p>
  <strong><%= t("activerecord.attributes.user.role_id") %>:</strong>
  <%= handle_field_value(@user.role_id) %>
</p>

<%= link_to 'Edit', edit_contact_path(@user) %> |
<%= link_to 'Back', contacts_path %>
